// The joa capstone case files, generated by tools/course-waves/joa/gen_course.py
// from the engine's own inputs (joa_capstone.mjs --inputs). Only the capstone card
// imports this module: no panel preloads a case, and the graded answers are not
// stored anywhere in the app.
import idumu_case from './idumu_case.json?raw';
import okwelle_case from './okwelle_case.json?raw';
import abiama_case from './abiama_case.json?raw';

export const JOA_CASE_FILES = {
  beginner: [
    { name: 'idumu_case.json', text: idumu_case },
  ],
  intermediate: [
    { name: 'okwelle_case.json', text: okwelle_case },
  ],
  advanced: [
    { name: 'abiama_case.json', text: abiama_case },
  ],
};
