// The pia capstone case files, generated by tools/course-waves/pia/gen_course.py
// from the engine's own inputs (pia_capstone.mjs --inputs). Only the capstone card
// imports this module: no panel preloads a case, and the graded answers are not
// stored anywhere in the app.
import odozi_case from './odozi_case.json?raw';
import nkemdi_case from './nkemdi_case.json?raw';
import alaku_case from './alaku_case.json?raw';

export const PIA_CASE_FILES = {
  beginner: [
    { name: 'odozi_case.json', text: odozi_case },
  ],
  intermediate: [
    { name: 'nkemdi_case.json', text: nkemdi_case },
  ],
  advanced: [
    { name: 'alaku_case.json', text: alaku_case },
  ],
};
