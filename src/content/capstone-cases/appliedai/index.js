// The appliedai capstone case files, generated by tools/course-waves/appliedai/gen_course.py
// from the engine's own inputs (d5_capstone.mjs --inputs). Only the capstone card
// imports this module: no panel preloads a case, and the graded answers are not
// stored anywhere in the app.
import orlu_case from './orlu_case.json?raw';
import nnewi_case from './nnewi_case.json?raw';
import awka_case from './awka_case.json?raw';

export const APPLIEDAI_CASE_FILES = {
  beginner: [
    { name: 'orlu_case.json', text: orlu_case },
  ],
  intermediate: [
    { name: 'nnewi_case.json', text: nnewi_case },
  ],
  advanced: [
    { name: 'awka_case.json', text: awka_case },
  ],
};
