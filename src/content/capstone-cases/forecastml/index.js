// The forecastml capstone case files, generated by tools/course-waves/forecastml/gen_course.py
// from the engine's own inputs (d4_capstone.mjs --inputs). Only the capstone card
// imports this module: no panel preloads a case, and the graded answers are not
// stored anywhere in the app.
import agulu_rates from './agulu_rates.csv?raw';
import nanka_rates from './nanka_rates.csv?raw';
import umunze_rates from './umunze_rates.csv?raw';

export const FORECASTML_CASE_FILES = {
  beginner: [
    { name: 'agulu_rates.csv', text: agulu_rates },
  ],
  intermediate: [
    { name: 'nanka_rates.csv', text: nanka_rates },
  ],
  advanced: [
    { name: 'umunze_rates.csv', text: umunze_rates },
  ],
};
