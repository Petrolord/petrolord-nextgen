// The mlcore capstone case files, generated by tools/course-waves/mlcore/gen_course.py
// from the engine's own inputs (d2_capstone.mjs --inputs). Only the capstone card
// imports this module: no panel preloads a case, and the graded answers are not
// stored anywhere in the app.
import akpara_wells from './akpara_wells.csv?raw';
import oboria_wells from './oboria_wells.csv?raw';
import isuama_wells from './isuama_wells.csv?raw';

export const MLCORE_CASE_FILES = {
  beginner: [
    { name: 'akpara_wells.csv', text: akpara_wells },
  ],
  intermediate: [
    { name: 'oboria_wells.csv', text: oboria_wells },
  ],
  advanced: [
    { name: 'isuama_wells.csv', text: isuama_wells },
  ],
};
