-- ==========================================================================
-- B4 FOLLOW-ON FIX (completion): B4 follow-on: completion double-key, drift mandrel wording and duplicate fixes
--
-- Courses: completion. Rows changed: 5; rows inserted: 0.
-- Source: tools/answer-length-audit/fixes/completion.json (every item, its verdict and
-- its reason, old and new text side by side); generated by
-- tools/answer-length-audit/fix.py sql completion. Runs AFTER the 29 B4 length files
-- and any earlier fix file: every guard below is the text those leave behind.
--
-- GUARDS. Each changed row must match its OLD prompt, explanation and options
-- (md5) and answer_index exactly (then it is updated) or its NEW ones (already
-- applied, left alone). Anything else raises and the transaction rolls back.
-- Each insert must be absent (inserted) or present with the same text (left).
-- Each course ends at its asserted question count. SAFE TO RE-RUN.
-- ==========================================================================

do $$
declare
  v_state   text;
  v_count   integer;
  v_updated integer := 0;
  v_total   integer;
begin

  -- completion advanced final - ord 35
  select case when md5(prompt) = '3c7d1d61e5b66e5d66942aa5e890b76e' and md5(coalesce(explanation, '')) = '863b1d91f10118c48ce4f95c9075dd0a' and md5(options::text) = md5('["The drift table, whose deduction values are looked up in the specification for each pipe size.", "The through bore, which uses only the inside diameters and therefore does not depend on the approximate outside ones.", "The space out, which reads a bore length, a landing and two length changes.", "The volumes, which are computed from lengths and casing bores rather than from any equipment dimension at all."]'::jsonb::text) and answer_index = 2 then 'old'
              when md5(prompt) = 'ec3aa261411de96e052497e814ef6748' and md5(coalesce(explanation, '')) = '863b1d91f10118c48ce4f95c9075dd0a' and md5(options::text) = md5('["The drift table, whose deduction values are looked up in the specification for each pipe size.", "The through bore, which uses only the inside diameters and therefore does not depend on the approximate outside ones.", "The space out, which reads a bore length, a landing and two length changes.", "The volumes, which are computed from lengths and casing bores rather than from any equipment dimension at all."]'::jsonb::text) and answer_index = 2 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'completion' and tier = 'advanced' and scope = 'final' and module_key is not distinct from null and ord = 35 and active;
  if v_state is null then raise exception 'B4 fix completion refused: no row for completion advanced final - ord 35'; end if;
  if v_state = 'other' then raise exception 'B4 fix completion refused: completion advanced final - ord 35 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'Which calculation in this course depends on none of the catalog at all, neither the API pipe tables nor the approximate equipment rows?', options = '["The drift table, whose deduction values are looked up in the specification for each pipe size.", "The through bore, which uses only the inside diameters and therefore does not depend on the approximate outside ones.", "The space out, which reads a bore length, a landing and two length changes.", "The volumes, which are computed from lengths and casing bores rather than from any equipment dimension at all."]'::jsonb,
           answer_index = 2, explanation = 'Its results are unconditional: the identity, the band and the minimum bore length are arithmetic.'
     where app_slug = 'completion' and tier = 'advanced' and scope = 'final' and module_key is not distinct from null and ord = 35 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix completion refused: completion advanced final - ord 35 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- completion beginner final - ord 29
  select case when md5(prompt) = '172c99b79fcf1c2e6a513c014f119dcc' and md5(coalesce(explanation, '')) = '472bf3d71c6263a49e1fa8f0c470830a' and md5(options::text) = md5('["Nothing else; subtract the packer diameter from the drift and read the sign.", "The weight and grade of the casing.", "The packer setting depth, since the quoted drift may not govern there.", "The packer length, since a long component needs more clearance."]'::jsonb::text) and answer_index = 0 then 'old'
              when md5(prompt) = 'f65f544790a9643ea3cfdd9bd8fbfff5' and md5(coalesce(explanation, '')) = '472bf3d71c6263a49e1fa8f0c470830a' and md5(options::text) = md5('["Nothing else; subtract the packer diameter from the drift and read the sign.", "The weight and grade of the casing.", "The nominal bore of the casing, since the drift is deducted from it.", "The packer length, since a long component needs more clearance."]'::jsonb::text) and answer_index = 0 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'completion' and tier = 'beginner' and scope = 'final' and module_key is not distinct from null and ord = 29 and active;
  if v_state is null then raise exception 'B4 fix completion refused: no row for completion beginner final - ord 29'; end if;
  if v_state = 'other' then raise exception 'B4 fix completion refused: completion beginner final - ord 29 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'You are given only the drift of the single casing string a five and seven eighths inch packer will run through. What else do you need to say whether it passes?', options = '["Nothing else; subtract the packer diameter from the drift and read the sign.", "The weight and grade of the casing.", "The nominal bore of the casing, since the drift is deducted from it.", "The packer length, since a long component needs more clearance."]'::jsonb,
           answer_index = 0, explanation = 'At this tier the comparison is a subtraction. The governing drift question comes in the next tier.'
     where app_slug = 'completion' and tier = 'beginner' and scope = 'final' and module_key is not distinct from null and ord = 29 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix completion refused: completion beginner final - ord 29 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- completion beginner module m02-api-drift ord 12
  select case when md5(prompt) = 'b2e468082e87797e37c39919f7cfe783' and md5(coalesce(explanation, '')) = 'e86ac1b4ed570e8cef5f06567d485edc' and md5(options::text) = md5('["The bore of the same casing three years later, after scale and corrosion.", "A twelve inch mandrel passing through a new joint that has never been in a well.", "The dimensional acceptability of the joint against the manufacturing tolerance stated in the specification for that size.", "A rigid cylinder of the drift diameter and the mandrel length passing through the joint at the mill on the day it was tested."]'::jsonb::text) and answer_index = 0 then 'old'
              when md5(prompt) = 'b2e468082e87797e37c39919f7cfe783' and md5(coalesce(explanation, '')) = 'e86ac1b4ed570e8cef5f06567d485edc' and md5(options::text) = md5('["The bore of the same casing three years later, after scale and corrosion.", "The standard drift mandrel for that pipe size passing through a new joint never run in a well.", "The dimensional acceptability of the joint against the manufacturing tolerance stated in the specification for that size.", "A rigid cylinder of the drift diameter and the mandrel length passing through the joint at the mill on the day it was tested."]'::jsonb::text) and answer_index = 0 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'completion' and tier = 'beginner' and scope = 'module' and module_key is not distinct from 'm02-api-drift' and ord = 12 and active;
  if v_state is null then raise exception 'B4 fix completion refused: no row for completion beginner module m02-api-drift ord 12'; end if;
  if v_state = 'other' then raise exception 'B4 fix completion refused: completion beginner module m02-api-drift ord 12 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'Which of these does the drift guarantee NOT cover?', options = '["The bore of the same casing three years later, after scale and corrosion.", "The standard drift mandrel for that pipe size passing through a new joint never run in a well.", "The dimensional acceptability of the joint against the manufacturing tolerance stated in the specification for that size.", "A rigid cylinder of the drift diameter and the mandrel length passing through the joint at the mill on the day it was tested."]'::jsonb,
           answer_index = 0, explanation = 'The catalog value says what to expect and a drift run says what is actually there.'
     where app_slug = 'completion' and tier = 'beginner' and scope = 'module' and module_key is not distinct from 'm02-api-drift' and ord = 12 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix completion refused: completion beginner module m02-api-drift ord 12 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- completion intermediate module m03-getting-through-it-afterwards ord 14
  select case when md5(prompt) = '7759582737de3342209f9e17d823888c' and md5(coalesce(explanation, '')) = 'b9b1b4ae3714f1540b0536d1d6590230' and md5(options::text) = md5('["Never; it should always be reported with its extent.", "When every component in the string has the same bore throughout.", "When the operation has to reach the bottom, where the extreme is the binding constraint.", "When the restriction governs more than half the string, so that quoting it alone is representative of the whole of it."]'::jsonb::text) and answer_index = 2 then 'old'
              when md5(prompt) = '7759582737de3342209f9e17d823888c' and md5(coalesce(explanation, '')) = 'b9b1b4ae3714f1540b0536d1d6590230' and md5(options::text) = md5('["Never; it should always be reported with its extent.", "When the tool only has to reach a depth above the smallest bore.", "When the operation has to reach the bottom, where the extreme is the binding constraint.", "When the restriction governs more than half the string, so that quoting it alone is representative of the whole of it."]'::jsonb::text) and answer_index = 2 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'completion' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm03-getting-through-it-afterwards' and ord = 14 and active;
  if v_state is null then raise exception 'B4 fix completion refused: no row for completion intermediate module m03-getting-through-it-afterwards ord 14'; end if;
  if v_state = 'other' then raise exception 'B4 fix completion refused: completion intermediate module m03-getting-through-it-afterwards ord 14 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'When is the headline through bore the right summary on its own?', options = '["Never; it should always be reported with its extent.", "When the tool only has to reach a depth above the smallest bore.", "When the operation has to reach the bottom, where the extreme is the binding constraint.", "When the restriction governs more than half the string, so that quoting it alone is representative of the whole of it."]'::jsonb,
           answer_index = 2, explanation = 'A plug set in the no go nipple has to pass the no go nipple, whatever the rest of the string looks like.'
     where app_slug = 'completion' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm03-getting-through-it-afterwards' and ord = 14 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix completion refused: completion intermediate module m03-getting-through-it-afterwards ord 14 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- completion intermediate module m04-the-volumes ord 12
  select case when md5(prompt) = 'fc3a086c499dd95202130e977bc119e3' and md5(coalesce(explanation, '')) = '08c6d94c25cf4f5cfbbf5c6a2f42c266' and md5(options::text) = md5('["It refuses the calculation and returns nothing, since a partial volume would mislead.", "It uses the bore of the nearest covered interval, which keeps the volume complete at the cost of a small unquantified error.", "It returns the volume computed over the covered intervals only, with no indication that anything at all has been left out.", "It skips the interval, continues, and adds a warning naming the depths."]'::jsonb::text) and answer_index = 3 then 'old'
              when md5(prompt) = '515160f0e9cfe671bf0eb8adcf61c54f' and md5(coalesce(explanation, '')) = '309321f12b6f924cff5abf2c0abe1950' and md5(options::text) = md5('["A refusal, since a bore that disagrees with the pipe tables fails validation before any integration starts.", "The volume over the correct intervals only, plus a warning naming the depths where the mistyped bore sits.", "A volume built from the nearest valid catalog bore, since the engine replaces any bore it cannot match to a pipe size.", "A complete looking volume that is wrong, with an empty warning list."]'::jsonb::text) and answer_index = 3 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'completion' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm04-the-volumes' and ord = 12 and active;
  if v_state is null then raise exception 'B4 fix completion refused: no row for completion intermediate module m04-the-volumes ord 12'; end if;
  if v_state = 'other' then raise exception 'B4 fix completion refused: completion intermediate module m04-the-volumes ord 12 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'A complete casing program is entered with a typo in one bore. What does the volume integration return?', options = '["A refusal, since a bore that disagrees with the pipe tables fails validation before any integration starts.", "The volume over the correct intervals only, plus a warning naming the depths where the mistyped bore sits.", "A volume built from the nearest valid catalog bore, since the engine replaces any bore it cannot match to a pipe size.", "A complete looking volume that is wrong, with an empty warning list."]'::jsonb,
           answer_index = 3, explanation = 'Warnings catch missing data and nothing here catches incorrect data, so a complete program with a typo gives a complete profile, no warnings and a wrong volume.'
     where app_slug = 'completion' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm04-the-volumes' and ord = 12 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix completion refused: completion intermediate module m04-the-volumes ord 12 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  select count(*) into v_total from public.academy_quiz_questions where app_slug = 'completion' and active;
  if v_total <> 396 then raise exception 'B4 fix completion refused: completion holds % active questions, expected 396', v_total; end if;
  raise notice 'B4 fix completion: % of 5 rows written, the rest already carried the fixed text', v_updated;
end $$;
