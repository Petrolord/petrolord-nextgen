-- ==========================================================================
-- ANSWER-LENGTH RECUT (B4): mapping, distractor text only.
--
-- WHY. A learner who never reads a question could pass this course by always
-- choosing the option at one length rank. Each row below lengthens or trims
-- DISTRACTORS so the correct option sits at every length rank in a fair
-- share (bankkit band: each rank 12 to 40 percent of a bank). The correct
-- option, answer_index, option order, prompt and explanation never change,
-- and no edit adds a number the option did not already carry.
-- Rows: 3 of the course's 396.
-- Source: tools/answer-length-audit/edits/mapping.json over the served rows;
-- generated by tools/answer-length-audit/recut.py sql mapping.
--
-- GUARDS. Each row is addressed by (app_slug, tier, scope, module_key, ord). Its
-- prompt and explanation must match by md5 and its answer_index exactly, and its
-- options must equal EITHER the published list (then it is updated) OR the recut
-- list (already applied, left alone). Anything else raises and the whole
-- transaction rolls back. Every update touches exactly 1 row and the course keeps
-- its 396 questions. SAFE TO RE-RUN.
-- ==========================================================================

do $$
declare
  v_state   text;
  v_count   integer;
  v_updated integer := 0;
  v_total   integer;
begin

  -- advanced module m02-leave-one-out ord 15
  select case
           when md5(prompt) <> '73ad3fdc89c393a7b492454a2ebd4ea7' or md5(coalesce(explanation, '')) <> 'afb8f11fa1c01dc9be3f212e7d7cb78f' or answer_index <> 0 then 'other'
           when options = '["The plain mean of plus 2.09 m.", "The mean absolute of 7.76 m.", "The root mean square of 8.03 m.", "Quoting both individually with their signs."]'::jsonb then 'old'
           when options = '["The plain mean of plus 2.09 m.", "The mean absolute of 7.76 m.", "The RMS of 8.03 m.", "Quoting both individually with their signs."]'::jsonb then 'new'
           else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'mapping' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm02-leave-one-out' and ord = 15;
  if v_state is null then raise exception 'answer-length recut, mapping refused: no row for advanced module m02-leave-one-out ord 15'; end if;
  if v_state = 'other' then raise exception 'answer-length recut, mapping refused: advanced module m02-leave-one-out ord 15 matches neither its published nor its recut form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set options = '["The plain mean of plus 2.09 m.", "The mean absolute of 7.76 m.", "The RMS of 8.03 m.", "Quoting both individually with their signs."]'::jsonb
     where app_slug = 'mapping' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm02-leave-one-out' and ord = 15;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'answer-length recut, mapping refused: advanced module m02-leave-one-out ord 15 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- intermediate module m03-reading-the-thickness-map ord 14
  select case
           when md5(prompt) <> '7b6e14c0642a8c9f86664d4ee50e8fd6' or md5(coalesce(explanation, '')) <> '88db12037247a012009fbecef209fe86' or answer_index <> 0 then 'other'
           when options = '["Because the coarsest grid, with 50 live nodes rather than 201, gets the finest interval.", "Because nine levels is below the ten the rule targets, which means the rule has failed on this range.", "Because a 1 m interval on a thickness map implies the picks were made to the decimetre.", "Because the 200 m grid has lost the maximum as well, so both ends of the range have moved."]'::jsonb then 'old'
           when options = '["Because the coarsest grid, with 50 live nodes rather than 201, gets the finest interval.", "Because nine levels is below the ten the rule targets, which means the rule has failed on this range.", "Because a 1 m interval on a thickness map implies the picks were made to the decimetre.", "Because the 200 m grid lost the maximum too, so both ends of the range have moved."]'::jsonb then 'new'
           else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'mapping' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm03-reading-the-thickness-map' and ord = 14;
  if v_state is null then raise exception 'answer-length recut, mapping refused: no row for intermediate module m03-reading-the-thickness-map ord 14'; end if;
  if v_state = 'other' then raise exception 'answer-length recut, mapping refused: intermediate module m03-reading-the-thickness-map ord 14 matches neither its published nor its recut form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set options = '["Because the coarsest grid, with 50 live nodes rather than 201, gets the finest interval.", "Because nine levels is below the ten the rule targets, which means the rule has failed on this range.", "Because a 1 m interval on a thickness map implies the picks were made to the decimetre.", "Because the 200 m grid lost the maximum too, so both ends of the range have moved."]'::jsonb
     where app_slug = 'mapping' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm03-reading-the-thickness-map' and ord = 14;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'answer-length recut, mapping refused: intermediate module m03-reading-the-thickness-map ord 14 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- intermediate module m06-the-isochore-workflow ord 14
  select case
           when md5(prompt) <> 'a451eb9b010bb3fee569466018d889b4' or md5(coalesce(explanation, '')) <> '8e046c635e1dda3c34dc22c6eec5ad45' or answer_index <> 3 then 'other'
           when options = '["How much the mapped surface changes when the cell size is halved and the mask is held fixed.", "How far the map mean moves when the least reliable well is removed from the control.", "How much of the mapped area lies more than 800 m from any control point.", "How far the map''s prediction at a withheld well falls from the pick that was withheld."]'::jsonb then 'old'
           when options = '["How much the mapped surface changes when the cell size is halved and the mask is held fixed.", "How far the map mean moves when the least reliable well is dropped from the control set and the rest regridded.", "How much of the mapped area lies more than 800 m from any control point, a coverage statistic for the grid.", "How far the map''s prediction at a withheld well falls from the pick that was withheld."]'::jsonb then 'new'
           else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'mapping' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm06-the-isochore-workflow' and ord = 14;
  if v_state is null then raise exception 'answer-length recut, mapping refused: no row for intermediate module m06-the-isochore-workflow ord 14'; end if;
  if v_state = 'other' then raise exception 'answer-length recut, mapping refused: intermediate module m06-the-isochore-workflow ord 14 matches neither its published nor its recut form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set options = '["How much the mapped surface changes when the cell size is halved and the mask is held fixed.", "How far the map mean moves when the least reliable well is dropped from the control set and the rest regridded.", "How much of the mapped area lies more than 800 m from any control point, a coverage statistic for the grid.", "How far the map''s prediction at a withheld well falls from the pick that was withheld."]'::jsonb
     where app_slug = 'mapping' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm06-the-isochore-workflow' and ord = 14;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'answer-length recut, mapping refused: intermediate module m06-the-isochore-workflow ord 14 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  select count(*) into v_total from public.academy_quiz_questions where app_slug = 'mapping';
  if v_total <> 396 then raise exception 'answer-length recut, mapping refused: the course holds % questions, expected 396', v_total; end if;
  raise notice 'answer-length recut, mapping: % of 3 rows updated, the rest already carried the recut text', v_updated;
end $$;
