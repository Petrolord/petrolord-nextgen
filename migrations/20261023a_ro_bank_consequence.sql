-- ==========================================================================
-- B4/B5 ROUND-OFF BANK FIX (ro_consequence): class B sign hit: the heptane k beta stated as the course prints it
--
-- Courses: consequence. Rows changed: 1; rows inserted: 0.
-- Source: tools/answer-length-audit/fixes/ro_consequence.json (every item, its verdict and
-- its reason, old and new text side by side); generated by
-- tools/answer-length-audit/fix.py sql ro_consequence. Runs AFTER the 29 B4 length files, the 37 B4 fix files (20261021a/b) and 20261022:
-- every guard below is the text those leave behind.
--
-- GUARDS. Each changed row must match its OLD prompt, explanation and options
-- (md5) and answer_index exactly (then it is updated) or its NEW ones (already
-- applied, left alone). Anything else raises and the transaction rolls back.
-- Each insert must be absent (inserted) or present with the same text (left).
-- Each course ends at its asserted question count. SAFE TO RE-RUN.
-- ==========================================================================

do $$
declare
  v_state   text;
  v_count   integer;
  v_updated integer := 0;
  v_total   integer;
begin

  -- consequence intermediate module m01-how-fast-a-pool-burns ord 2
  select case when md5(prompt) = '98bcc9d904a65d9fe88dc1ca97f42447' and md5(coalesce(explanation, '')) = '412fa7213b9ad6b8b2065086a1288368' and md5(options::text) = md5('["0.067380", "0.074587", "0.052036", "0.089809"]'::jsonb::text) and answer_index = 0 then 'old'
              when md5(prompt) = '98bcc9d904a65d9fe88dc1ca97f42447' and md5(coalesce(explanation, '')) = 'd7d626a81c18688f4069d02f6bd20d03' and md5(options::text) = md5('["0.067380", "0.074587", "0.052036", "0.089809"]'::jsonb::text) and answer_index = 0 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'consequence' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm01-how-fast-a-pool-burns' and ord = 2 and active;
  if v_state is null then raise exception 'roundoff bank consequence refused: no row for consequence intermediate module m01-how-fast-a-pool-burns ord 2'; end if;
  if v_state = 'other' then raise exception 'roundoff bank consequence refused: consequence intermediate module m01-how-fast-a-pool-burns ord 2 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'Using the Babrauskas form with the Table 6.5 heptane row, what burning flux in kg/(m2 s) does the engine return for a heptane pool of 1 m diameter?', options = '["0.067380", "0.074587", "0.052036", "0.089809"]'::jsonb,
           answer_index = 0, explanation = 'The burning flux table prints 0.067380 for heptane at 1 m, from m"inf 0.101 times one minus exp(-k beta D), with the heptane k beta of 1.1 per m. 0.074587 is the lpg column at the same diameter, whose k beta of 1.4 brings it closer to its asymptote. 0.052036 is lng at 1 m. 0.089809 is heptane again at 2 m, where the larger diameter has pushed it further up its curve.'
     where app_slug = 'consequence' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm01-how-fast-a-pool-burns' and ord = 2 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'roundoff bank consequence refused: consequence intermediate module m01-how-fast-a-pool-burns ord 2 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  select count(*) into v_total from public.academy_quiz_questions where app_slug = 'consequence' and active;
  if v_total <> 396 then raise exception 'roundoff bank consequence refused: consequence holds % active questions, expected 396', v_total; end if;
  raise notice 'roundoff bank consequence: % of 1 rows written, the rest already carried the fixed text', v_updated;
end $$;
