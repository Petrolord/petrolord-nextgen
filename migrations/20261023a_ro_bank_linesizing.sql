-- ==========================================================================
-- B4/B5 ROUND-OFF BANK FIX (ro_linesizing): class A sign hit: a distractor no longer invents a bound on s
--
-- Courses: linesizing. Rows changed: 1; rows inserted: 0.
-- Source: tools/answer-length-audit/fixes/ro_linesizing.json (every item, its verdict and
-- its reason, old and new text side by side); generated by
-- tools/answer-length-audit/fix.py sql ro_linesizing. Runs AFTER the 29 B4 length files, the 37 B4 fix files (20261021a/b) and 20261022:
-- every guard below is the text those leave behind.
--
-- GUARDS. Each changed row must match its OLD prompt, explanation and options
-- (md5) and answer_index exactly (then it is updated) or its NEW ones (already
-- applied, left alone). Anything else raises and the transaction rolls back.
-- Each insert must be absent (inserted) or present with the same text (left).
-- Each course ends at its asserted question count. SAFE TO RE-RUN.
-- ==========================================================================

do $$
declare
  v_state   text;
  v_count   integer;
  v_updated integer := 0;
  v_total   integer;
begin

  -- linesizing intermediate module m03-the-elevation-adjustment ord 1
  select case when md5(prompt) = '51c060016b262e4d4b703bd2f04444ed' and md5(coalesce(explanation, '')) = '239eb93c1c99da6dbde64cb8249036ec' and md5(options::text) = md5('["That s is proportional to the elevation change only over falls, the rise column being the mirror image the engine writes rather than a value it computes separately.", "That s grows faster than the elevation change, which is the asymmetry between climbing and descending that the rest of the module is about.", "That s is proportional to the elevation change, so equal distances up and down give exactly opposite values and doubling the fall doubles s.", "That s is bounded between -1.0000000000 and 1.0000000000, which is what allows it to be handed to an exponential without the group overflowing on a tall hill."]'::jsonb::text) and answer_index = 2 then 'old'
              when md5(prompt) = '51c060016b262e4d4b703bd2f04444ed' and md5(coalesce(explanation, '')) = '239eb93c1c99da6dbde64cb8249036ec' and md5(options::text) = md5('["That s is proportional to the elevation change only over falls, the rise column being the mirror image the engine writes rather than a value it computes separately.", "That s grows faster than the elevation change, which is the asymmetry between climbing and descending that the rest of the module is about.", "That s is proportional to the elevation change, so equal distances up and down give exactly opposite values and doubling the fall doubles s.", "That s never exceeds one in size in either direction, which is what allows it to be handed to an exponential without the group overflowing on a tall hill."]'::jsonb::text) and answer_index = 2 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'linesizing' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm03-the-elevation-adjustment' and ord = 1 and active;
  if v_state is null then raise exception 'roundoff bank linesizing refused: no row for linesizing intermediate module m03-the-elevation-adjustment ord 1'; end if;
  if v_state = 'other' then raise exception 'roundoff bank linesizing refused: linesizing intermediate module m03-the-elevation-adjustment ord 1 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'On the SOKU trunk 1500.000000 ft of rise gives s as 0.0795976556 and 1500.000000 ft of fall gives -0.0795976556, while 3000.000000 ft of fall gives -0.1591953113. What property of s do those three rows establish?', options = '["That s is proportional to the elevation change only over falls, the rise column being the mirror image the engine writes rather than a value it computes separately.", "That s grows faster than the elevation change, which is the asymmetry between climbing and descending that the rest of the module is about.", "That s is proportional to the elevation change, so equal distances up and down give exactly opposite values and doubling the fall doubles s.", "That s never exceeds one in size in either direction, which is what allows it to be handed to an exponential without the group overflowing on a tall hill."]'::jsonb,
           answer_index = 2, explanation = 's itself is well behaved. Everything awkward about a hill happens after s, when it goes into an exponential.'
     where app_slug = 'linesizing' and tier = 'intermediate' and scope = 'module' and module_key is not distinct from 'm03-the-elevation-adjustment' and ord = 1 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'roundoff bank linesizing refused: linesizing intermediate module m03-the-elevation-adjustment ord 1 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  select count(*) into v_total from public.academy_quiz_questions where app_slug = 'linesizing' and active;
  if v_total <> 396 then raise exception 'roundoff bank linesizing refused: linesizing holds % active questions, expected 396', v_total; end if;
  raise notice 'roundoff bank linesizing: % of 1 rows written, the rest already carried the fixed text', v_updated;
end $$;
