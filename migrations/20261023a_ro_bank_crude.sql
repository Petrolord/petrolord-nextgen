-- ==========================================================================
-- B4/B5 ROUND-OFF BANK FIX (ro_crude): cra_m01 Q9 explanation without the gloss #182 cut from the lessons
--
-- Courses: crude. Rows changed: 1; rows inserted: 0.
-- Source: tools/answer-length-audit/fixes/ro_crude.json (every item, its verdict and
-- its reason, old and new text side by side); generated by
-- tools/answer-length-audit/fix.py sql ro_crude. Runs AFTER the 29 B4 length files, the 37 B4 fix files (20261021a/b) and 20261022:
-- every guard below is the text those leave behind.
--
-- GUARDS. Each changed row must match its OLD prompt, explanation and options
-- (md5) and answer_index exactly (then it is updated) or its NEW ones (already
-- applied, left alone). Anything else raises and the transaction rolls back.
-- Each insert must be absent (inserted) or present with the same text (left).
-- Each course ends at its asserted question count. SAFE TO RE-RUN.
-- ==========================================================================

do $$
declare
  v_state   text;
  v_count   integer;
  v_updated integer := 0;
  v_total   integer;
begin

  -- crude advanced module m01-what-a-linear-programme-is ord 9
  select case when md5(prompt) = 'e890e0263a693751afb119feaee58213' and md5(coalesce(explanation, '')) = 'eca1f38485d9a1d75f2b1ac1dc9c40d3' and md5(options::text) = md5('["As infeasible, since the short row contradicts the rest.", "As unbounded, since one variable is free of that row.", "As optimal on the one variable the short row names.", "As a fault in the question, with no status attached to it at all."]'::jsonb::text) and answer_index = 3 then 'old'
              when md5(prompt) = 'e890e0263a693751afb119feaee58213' and md5(coalesce(explanation, '')) = 'cf553dc2f8e2f448546100457b83e1d8' and md5(options::text) = md5('["As infeasible, since the short row contradicts the rest.", "As unbounded, since one variable is free of that row.", "As optimal on the one variable the short row names.", "As a fault in the question, with no status attached to it at all."]'::jsonb::text) and answer_index = 3 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'crude' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm01-what-a-linear-programme-is' and ord = 9 and active;
  if v_state is null then raise exception 'roundoff bank crude refused: no row for crude advanced module m01-what-a-linear-programme-is ord 9'; end if;
  if v_state = 'other' then raise exception 'roundoff bank crude refused: crude advanced module m01-what-a-linear-programme-is ord 9 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'A row is written with 1 coefficient on a problem with two variables, and the kernel stops with "Row 0 has 1 coefficients, expected 2". How is that result to be read?', options = '["As infeasible, since the short row contradicts the rest.", "As unbounded, since one variable is free of that row.", "As optimal on the one variable the short row names.", "As a fault in the question, with no status attached to it at all."]'::jsonb,
           answer_index = 3, explanation = 'A problem that is written wrongly is neither optimal, infeasible nor unbounded. The kernel throws, which is a fault in the question, and it refuses to answer it with a status.'
     where app_slug = 'crude' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm01-what-a-linear-programme-is' and ord = 9 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'roundoff bank crude refused: crude advanced module m01-what-a-linear-programme-is ord 9 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  select count(*) into v_total from public.academy_quiz_questions where app_slug = 'crude' and active;
  if v_total <> 396 then raise exception 'roundoff bank crude refused: crude holds % active questions, expected 396', v_total; end if;
  raise notice 'roundoff bank crude: % of 1 rows written, the rest already carried the fixed text', v_updated;
end $$;
