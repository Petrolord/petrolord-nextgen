-- ==========================================================================
-- B4 FOLLOW-ON FIX (wellcost): wellcost: two arguably-correct distractors rewritten; one flag judged fine
--
-- Courses: wellcost. Rows changed: 3; rows inserted: 0.
-- Source: tools/answer-length-audit/fixes/wellcost.json (every item, its verdict and
-- its reason, old and new text side by side); generated by
-- tools/answer-length-audit/fix.py sql wellcost. Runs AFTER the 29 B4 length files
-- and any earlier fix file: every guard below is the text those leave behind.
--
-- GUARDS. Each changed row must match its OLD prompt, explanation and options
-- (md5) and answer_index exactly (then it is updated) or its NEW ones (already
-- applied, left alone). Anything else raises and the transaction rolls back.
-- Each insert must be absent (inserted) or present with the same text (left).
-- Each course ends at its asserted question count. SAFE TO RE-RUN.
-- ==========================================================================

do $$
declare
  v_state   text;
  v_count   integer;
  v_updated integer := 0;
  v_total   integer;
begin

  -- wellcost advanced module m01-the-cost-time-curve ord 13
  select case when md5(prompt) = '5b81d61dbede927b6c6277144f54fac4' and md5(coalesce(explanation, '')) = '2dcaa291e83bcecead6848c2ad74f740' and md5(options::text) = md5('["It is running late and the per day lines keep billing, or it is on depth and simply spending faster.", "Either the contingency has been drawn down or the base was raised.", "Either the rig has been off contract for part of the period or the operator has chosen to pay several of the lump lines earlier than the programme assumed they would fall.", "Either the schedule slipped or the non-productive allowance was set too low, so extra rig hours are being billed."]'::jsonb::text) and answer_index = 0 then 'old'
              when md5(prompt) = '5b81d61dbede927b6c6277144f54fac4' and md5(coalesce(explanation, '')) = '2dcaa291e83bcecead6848c2ad74f740' and md5(options::text) = md5('["It is running late and the per day lines keep billing, or it is on depth and simply spending faster.", "Either the contingency has been drawn down or the base was raised.", "Either the rig has been off contract for part of the period or the operator has chosen to pay several of the lump lines earlier than the programme assumed they would fall.", "Either it has not yet drilled the section or it is behind on depth, so the money it has not spent is still owed."]'::jsonb::text) and answer_index = 0 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'wellcost' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm01-the-cost-time-curve' and ord = 13 and active;
  if v_state is null then raise exception 'B4 fix wellcost refused: no row for wellcost advanced module m01-the-cost-time-curve ord 13'; end if;
  if v_state = 'other' then raise exception 'B4 fix wellcost refused: wellcost advanced module m01-the-cost-time-curve ord 13 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'A well sits above the plan line at a given hour. What are the two quite different stories that can put it there?', options = '["It is running late and the per day lines keep billing, or it is on depth and simply spending faster.", "Either the contingency has been drawn down or the base was raised.", "Either the rig has been off contract for part of the period or the operator has chosen to pay several of the lump lines earlier than the programme assumed they would fall.", "Either it has not yet drilled the section or it is behind on depth, so the money it has not spent is still owed."]'::jsonb,
           answer_index = 0, explanation = 'One story compounds with every further day and the other does not, which is why the depth track has to sit beside it.'
     where app_slug = 'wellcost' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm01-the-cost-time-curve' and ord = 13 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix wellcost refused: wellcost advanced module m01-the-cost-time-curve ord 13 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- wellcost advanced module m05-reading-a-risked-result ord 13
  select case when md5(prompt) = '7e5391a2967010d820c41152426503c7' and md5(coalesce(explanation, '')) = '752aa96f25e48885d67c436c2f72031a' and md5(options::text) = md5('["Whether the engine evaluated each realisation correctly when it rolled up the nine cost lines for every draw of the run.", "Whether the seed and iteration count were recorded, which is the one part of a risked result the run itself cannot report back to you.", "Whether the totals were sorted before the percentiles were read off them, since an unsorted column gives meaningless percentiles.", "Whether the ranges fed into it were right, since nothing in the run compares an assumption against anything real."]'::jsonb::text) and answer_index = 3 then 'old'
              when md5(prompt) = '7e5391a2967010d820c41152426503c7' and md5(coalesce(explanation, '')) = '752aa96f25e48885d67c436c2f72031a' and md5(options::text) = md5('["Whether the mean sits above or below the median, since percentiles read off a sorted list by index cannot give the average of the draws.", "Whether the seed and iteration count were recorded, which is the one part of a risked result the run itself cannot report back to you.", "Whether the totals were sorted before the percentiles were read off them, since an unsorted column gives meaningless percentiles.", "Whether the ranges fed into it were right, since nothing in the run compares an assumption against anything real."]'::jsonb::text) and answer_index = 3 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'wellcost' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm05-reading-a-risked-result' and ord = 13 and active;
  if v_state is null then raise exception 'B4 fix wellcost refused: no row for wellcost advanced module m05-reading-a-risked-result ord 13'; end if;
  if v_state = 'other' then raise exception 'B4 fix wellcost refused: wellcost advanced module m05-reading-a-risked-result ord 13 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'A converged Monte Carlo run reports percentiles to the dollar. Which question does it leave completely unanswered?', options = '["Whether the mean sits above or below the median, since percentiles read off a sorted list by index cannot give the average of the draws.", "Whether the seed and iteration count were recorded, which is the one part of a risked result the run itself cannot report back to you.", "Whether the totals were sorted before the percentiles were read off them, since an unsorted column gives meaningless percentiles.", "Whether the ranges fed into it were right, since nothing in the run compares an assumption against anything real."]'::jsonb,
           answer_index = 3, explanation = 'The fixture''s coefficient of variation of 0.041744022345010456 is a property of four chosen triangulars, not a measurement of how variable a well is.'
     where app_slug = 'wellcost' and tier = 'advanced' and scope = 'module' and module_key is not distinct from 'm05-reading-a-risked-result' and ord = 13 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix wellcost refused: wellcost advanced module m05-reading-a-risked-result ord 13 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  -- wellcost intermediate final - ord 30
  select case when md5(prompt) = '976baee8e04b90b95f708776332d0124' and md5(coalesce(explanation, '')) = 'e8e8f259ddfd11fcc43cfee7560dac9e' and md5(options::text) = md5('["One hundred rotating hours, four on the flat allowance, and twelve for the round trip, giving 116 hours of rig time.", "Three hundred rotating, four flat and six on the trip.", "One hundred rotating hours, four on the allowance and sixteen on the trip, as the published fixture carries.", "One hundred rotating hours, four flat and twenty four for the trip, the round trip factor being applied twice over."]'::jsonb::text) and answer_index = 0 then 'old'
              when md5(prompt) = '976baee8e04b90b95f708776332d0124' and md5(coalesce(explanation, '')) = 'ce7f26cc185a8186a53924bf4c82549e' and md5(options::text) = md5('["One hundred rotating hours, four on the flat allowance, and twelve for the round trip, giving 116 hours of rig time.", "Three hundred rotating, four flat and six on the trip.", "One hundred rotating hours, four on the allowance and sixteen on the trip, as the published fixture carries.", "One hundred rotating hours, four flat and twenty four for the trip, the round trip factor being applied twice over."]'::jsonb::text) and answer_index = 0 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'wellcost' and tier = 'intermediate' and scope = 'final' and module_key is not distinct from null and ord = 30 and active;
  if v_state is null then raise exception 'B4 fix wellcost refused: no row for wellcost intermediate final - ord 30'; end if;
  if v_state = 'other' then raise exception 'B4 fix wellcost refused: wellcost intermediate final - ord 30 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'The production section cuts 1,000 m at 10 m/hr, takes the flat allowance, and makes its round trip from 3,000 m at 500 m/hr. What are its three hour terms?', options = '["One hundred rotating hours, four on the flat allowance, and twelve for the round trip, giving 116 hours of rig time.", "Three hundred rotating, four flat and six on the trip.", "One hundred rotating hours, four on the allowance and sixteen on the trip, as the published fixture carries.", "One hundred rotating hours, four flat and twenty four for the trip, the round trip factor being applied twice over."]'::jsonb,
           answer_index = 0, explanation = 'Drilling is 1,000 m at 10 m/hr, which is 100 hours; the flat connection allowance is 4 hours; and the round trip is twice 3,000 m over 500 m/hr, which is 12 hours. The 16 hour trip belongs to the published fixture, which is no section of this well. Every hour in the form is productive, so none of the three carries the non productive allowance.'
     where app_slug = 'wellcost' and tier = 'intermediate' and scope = 'final' and module_key is not distinct from null and ord = 30 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix wellcost refused: wellcost intermediate final - ord 30 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  select count(*) into v_total from public.academy_quiz_questions where app_slug = 'wellcost' and active;
  if v_total <> 396 then raise exception 'B4 fix wellcost refused: wellcost holds % active questions, expected 396', v_total; end if;
  raise notice 'B4 fix wellcost: % of 3 rows written, the rest already carried the fixed text', v_updated;
end $$;
