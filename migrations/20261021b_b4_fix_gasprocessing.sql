-- ==========================================================================
-- B4 FOLLOW-ON FIX (gasprocessing): gasprocessing: duplicate question rewritten into a new question
--
-- Courses: gasprocessing. Rows changed: 1; rows inserted: 0.
-- Source: tools/answer-length-audit/fixes/gasprocessing.json (every item, its verdict and
-- its reason, old and new text side by side); generated by
-- tools/answer-length-audit/fix.py sql gasprocessing. Runs AFTER the 29 B4 length files
-- and any earlier fix file: every guard below is the text those leave behind.
--
-- GUARDS. Each changed row must match its OLD prompt, explanation and options
-- (md5) and answer_index exactly (then it is updated) or its NEW ones (already
-- applied, left alone). Anything else raises and the transaction rolls back.
-- Each insert must be absent (inserted) or present with the same text (left).
-- Each course ends at its asserted question count. SAFE TO RE-RUN.
-- ==========================================================================

do $$
declare
  v_state   text;
  v_count   integer;
  v_updated integer := 0;
  v_total   integer;
begin

  -- gasprocessing advanced final - ord 34
  select case when md5(prompt) = 'f23298dd895c3b612252fcc31492afa1' and md5(coalesce(explanation, '')) = 'bd353bafc72f19ea3f227f35131874ca' and md5(options::text) = md5('["By dividing the gas constant by the standard pressure and multiplying by the standard temperature, which are all exported.", "By taking the saturation answer over the mole fraction the same call returns, which leaves the constant alone.", "By running the BTEX mole balance at one MMscfd, a million ppmv, a unit absorbed fraction and a unit molecular weight, where the answer is a million over the constant and nothing else.", "By comparing the engine''s water content against the golden''s, since the golden builds the same volume from the SI gas constant."]'::jsonb::text) and answer_index = 2 then 'old'
              when md5(prompt) = '0a71f1494b1ac14a014213cc96879119' and md5(coalesce(explanation, '')) = '63e755500559d67588cdf597edd15098' and md5(options::text) = md5('["By dividing the overhead term at a reflux ratio of 0.250000 by the water a gallon carries, which leaves the constant alone.", "By running the BTEX mole balance at a unit absorbed fraction, where the still overhead in pounds a day is the constant.", "By running the reboiler at a circulation ratio of one gallon per pound with no reflux, where the vaporization term is the overhead alone and nothing else.", "By comparing the reboiler duty against the golden''s, since the golden builds the same overhead from a steam table value."]'::jsonb::text) and answer_index = 2 then 'new'
              else 'other' end
    into v_state
    from public.academy_quiz_questions where app_slug = 'gasprocessing' and tier = 'advanced' and scope = 'final' and module_key is not distinct from null and ord = 34 and active;
  if v_state is null then raise exception 'B4 fix gasprocessing refused: no row for gasprocessing advanced final - ord 34'; end if;
  if v_state = 'other' then raise exception 'B4 fix gasprocessing refused: gasprocessing advanced final - ord 34 matches neither its current nor its fixed form'; end if;
  if v_state = 'old' then
    update public.academy_quiz_questions set prompt = 'How was the water overhead of 1100.000000000000 Btu a lb measured out of the engine rather than read from its export?', options = '["By dividing the overhead term at a reflux ratio of 0.250000 by the water a gallon carries, which leaves the constant alone.", "By running the BTEX mole balance at a unit absorbed fraction, where the still overhead in pounds a day is the constant.", "By running the reboiler at a circulation ratio of one gallon per pound with no reflux, where the vaporization term is the overhead alone and nothing else.", "By comparing the reboiler duty against the golden''s, since the golden builds the same overhead from a steam table value."]'::jsonb,
           answer_index = 2, explanation = 'To measure a constant, ask the engine a question whose answer is that constant and nothing else. At one gallon per pound a gallon carries one pound of water, and with no reflux the overhead is charged once, so the term reads the Btu a lb directly. A reflux ratio of 0.250000 adds a quarter on top, and the BTEX still overhead is aromatics.'
     where app_slug = 'gasprocessing' and tier = 'advanced' and scope = 'final' and module_key is not distinct from null and ord = 34 and active;
    get diagnostics v_count = row_count;
    if v_count <> 1 then raise exception 'B4 fix gasprocessing refused: gasprocessing advanced final - ord 34 updated % rows', v_count; end if;
    v_updated := v_updated + 1;
  end if;

  select count(*) into v_total from public.academy_quiz_questions where app_slug = 'gasprocessing' and active;
  if v_total <> 396 then raise exception 'B4 fix gasprocessing refused: gasprocessing holds % active questions, expected 396', v_total; end if;
  raise notice 'B4 fix gasprocessing: % of 1 rows written, the rest already carried the fixed text', v_updated;
end $$;
